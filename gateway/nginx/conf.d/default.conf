server {
    listen 80;
    server_name www.jigu.dev;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name www.jigu.dev;

    ssl_certificate /etc/letsencrypt/live/jigu.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/jigu.dev/privkey.pem;

    location / {
        proxy_pass http://jigu-core:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ──────────────────────────────────────────────
# 서브도메인 추가 예시: blog.jigu.dev -> jigu-blog:8080
# ──────────────────────────────────────────────
# 인증서 발급:
#   docker exec certbot certbot certonly --webroot \
#     -w /var/www/certbot -d blog.jigu.dev \
#     --email your@email.com --agree-tos
#
# server {
#     listen 80;
#     server_name blog.jigu.dev;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl;
#     server_name blog.jigu.dev;
#
#     ssl_certificate /etc/letsencrypt/live/blog.jigu.dev/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/blog.jigu.dev/privkey.pem;
#
#     location / {
#         proxy_pass http://jigu-blog:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
